---
interface Props {
  link: string;
  icon: string;
}
const { link, icon } = Astro.props;
---

<a class="text-2xl hover:text-neutral-50 transition duration-200" aria-label="link" target="_blank" href={`https://${link}`}
  ><i class={icon}></i></a
>
